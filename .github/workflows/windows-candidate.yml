name: Windows core build
run-name: "Windows core build ${{ github.sha }} #${{ github.run_number }}"

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  attestations: write

jobs:
  build:
    name: Build non-promotable Windows core artifact
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
      - name: Checkout mirror snapshot
        uses: actions/checkout@v6

      - name: Install pinned Rust toolchain
        shell: pwsh
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          $toolchain = (Get-Content rust-toolchain.toml | Select-String 'channel = "([^"]+)"').Matches.Groups[1].Value
          rustup toolchain install $toolchain --profile minimal --component rustfmt --component clippy
          rustup override set $toolchain
          rustc --version
          cargo --version

      - name: Test workspace and build release binaries
        shell: pwsh
        env:
          FAIRYPAM_BUILD_ID: public-core-${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          $env:FAIRYPAM_BUILD_COMMIT = (Get-Content SOURCE_COMMIT -Raw).Trim()
          cargo fmt --all -- --check
          cargo check --workspace --all-targets --locked
          cargo test --workspace --locked
          cargo clippy --workspace --all-targets --locked -- -D warnings
          cargo build --workspace --release --locked

      - name: Verify production Dev isolation
        shell: bash
        run: scripts/test-production-dev-isolation.sh target/release/fairypam-agent.exe target/release/fairypam-agentctl.exe target/release/fairypam-agent-guardian.exe

      - name: Package non-promotable core build
        shell: pwsh
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          New-Item -ItemType Directory -Force -Path dist/core | Out-Null
          Copy-Item target/release/fairypam-agent.exe dist/core/
          Copy-Item target/release/fairypam-agent-guardian.exe dist/core/
          Copy-Item -Recurse profiles dist/core/profiles
          Copy-Item test-profile-root-public-key.hex dist/core/
          Compress-Archive -Path dist/core/* -DestinationPath dist/fairypam-agent-core-windows.zip
          $sourceCommit = (Get-Content SOURCE_COMMIT -Raw).Trim()
          $agent = Get-Item dist/core/fairypam-agent.exe
          $guardian = Get-Item dist/core/fairypam-agent-guardian.exe
          $zip = Get-Item dist/fairypam-agent-core-windows.zip
          $receipt = [ordered]@{
            schema_version = 1
            artifact_class = 'core-build-only'
            promotable = $false
            source_commit = $sourceCommit
            public_commit = '${{ github.sha }}'
            build_id = 'public-core-${{ github.run_id }}-${{ github.run_attempt }}'
            run_id = '${{ github.run_id }}'
            run_attempt = '${{ github.run_attempt }}'
            agent_sha256 = (Get-FileHash $agent -Algorithm SHA256).Hash.ToLowerInvariant()
            agent_size = $agent.Length
            guardian_sha256 = (Get-FileHash $guardian -Algorithm SHA256).Hash.ToLowerInvariant()
            guardian_size = $guardian.Length
            zip_sha256 = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLowerInvariant()
            zip_size = $zip.Length
          }
          $receipt | ConvertTo-Json | Set-Content -Encoding utf8 dist/core-build-receipt.json

      - name: Attest core ZIP provenance
        uses: actions/attest@v4
        with:
          subject-path: dist/fairypam-agent-core-windows.zip

      - name: Attest core receipt provenance
        uses: actions/attest@v4
        with:
          subject-path: dist/core-build-receipt.json

      - name: Upload non-promotable core artifact
        uses: actions/upload-artifact@v7
        with:
          name: fairypam-agent-core-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            dist/fairypam-agent-core-windows.zip
            dist/core-build-receipt.json
          if-no-files-found: error
          retention-days: 14

  dev-automation:
    name: Build non-promotable Dev automation artifact
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - shell: pwsh
        name: Install pinned Rust toolchain
        run: |
          $toolchain = (Get-Content rust-toolchain.toml | Select-String 'channel = "([^"]+)"').Matches.Groups[1].Value
          rustup toolchain install $toolchain --profile minimal --component rustfmt --component clippy
          rustup override set $toolchain
      - shell: pwsh
        name: Build isolated Dev binaries
        env:
          FAIRYPAM_BUILD_ID: dev-${{ github.run_id }}-${{ github.run_attempt }}
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          $features = 'fairypam-agent/dev-automation,fairypam-agentctl/dev-automation,fairypam-agent-windows/testbed'
          cargo fmt --all -- --check
          cargo test --workspace --locked
          cargo test -p fairypam-agent --features dev-automation --locked
          cargo test -p fairypam-agentctl --features dev-automation --locked
          cargo clippy --workspace --all-targets --locked -- -D warnings
          cargo build --release --locked --no-default-features -p fairypam-agent -p fairypam-agent-guardian -p fairypam-agentctl -p fairypam-agent-windows --features $features
      - shell: bash
        name: Verify Dev installer contract
        run: scripts/test-dev-install-contract.sh
      - shell: pwsh
        name: Package Dev receipt
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          New-Item -ItemType Directory -Force dist/dev/profiles | Out-Null
          Copy-Item target/release/fairypam-agent.exe,target/release/fairypam-agent-guardian.exe,target/release/fairypam-agentctl.exe,target/release/fairypam-agent-testbed.exe,scripts/dev-install.ps1,scripts/dev-provision.ps1 dist/dev/
          Copy-Item -Recurse profiles/* dist/dev/profiles/
          Copy-Item test-profile-root-public-key.hex dist/dev/
          $root = (Resolve-Path dist/dev).Path
          $files = Get-ChildItem dist/dev -File -Recurse | ForEach-Object { [ordered]@{ path = $_.FullName.Substring($root.Length + 1).Replace('\','/'); sha256 = (Get-FileHash $_ -Algorithm SHA256).Hash.ToLowerInvariant(); size = $_.Length } }
          Compress-Archive -Path dist/dev/* -DestinationPath dist/fairypam-agent-dev-windows.zip
          $zip = Get-Item dist/fairypam-agent-dev-windows.zip
          [ordered]@{ schema_version = 1; artifact_class = 'dev-automation'; promotable = $false; source_commit = (Get-Content SOURCE_COMMIT -Raw).Trim(); public_commit = '${{ github.sha }}'; run_id = '${{ github.run_id }}'; run_attempt = '${{ github.run_attempt }}'; build_id = 'dev-${{ github.run_id }}-${{ github.run_attempt }}'; features = @('dev-automation','testbed'); files = @($files); zip_sha256 = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLowerInvariant(); zip_size = $zip.Length } | ConvertTo-Json -Depth 4 | Set-Content -Encoding utf8 dist/dev-build-receipt.json
      - uses: actions/attest@v4
        with: { subject-path: dist/fairypam-agent-dev-windows.zip }
      - uses: actions/attest@v4
        with: { subject-path: dist/dev-build-receipt.json }
      - uses: actions/upload-artifact@v7
        with:
          name: fairypam-agent-dev-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            dist/fairypam-agent-dev-windows.zip
            dist/dev-build-receipt.json
          if-no-files-found: error
          retention-days: 14

  tauri-ui:
    name: Build non-promotable unprivileged Tauri UI artifact
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6

      - name: Install Node for the Tauri UI
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: tauri-ui/package-lock.json

      - name: Install pinned Rust toolchain
        shell: pwsh
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          $toolchain = (Get-Content rust-toolchain.toml | Select-String 'channel = "([^"]+)"').Matches.Groups[1].Value
          rustup toolchain install $toolchain --profile minimal --component rustfmt --component clippy
          rustup override set $toolchain
          rustc --version
          cargo --version

      - name: Test and build the unprivileged Tauri UI
        shell: pwsh
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          Push-Location tauri-ui
          try {
            npm ci
            npm run build
            npm run check:security
            npm run check:command-surface
            npm test -- --run
            npm run test:accessibility

            Push-Location src-tauri
            try {
              cargo generate-lockfile
              git diff --exit-code -- Cargo.lock
              cargo fmt --all -- --check
              cargo check --all-targets --locked
              cargo test --all-targets --locked
              cargo clippy --all-targets --locked -- -D warnings
              cargo build --release --locked
            }
            finally {
              Pop-Location
            }
          }
          finally {
            Pop-Location
          }

      - name: Package unprivileged Tauri UI build
        shell: pwsh
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          New-Item -ItemType Directory -Force -Path dist/tauri-ui | Out-Null
          Copy-Item tauri-ui/src-tauri/target/release/fairypam-agent-tauri-ui.exe dist/tauri-ui/
          Copy-Item tauri-ui/src-tauri/Cargo.lock dist/tauri-ui/
          Compress-Archive -Path dist/tauri-ui/* -DestinationPath dist/fairypam-agent-tauri-ui-windows.zip
          $ui = Get-Item dist/tauri-ui/fairypam-agent-tauri-ui.exe
          $lock = Get-Item dist/tauri-ui/Cargo.lock
          $zip = Get-Item dist/fairypam-agent-tauri-ui-windows.zip
          [ordered]@{
            schema_version = 1
            artifact_class = 'tauri-ui-build-only'
            promotable = $false
            source_commit = (Get-Content SOURCE_COMMIT -Raw).Trim()
            public_commit = '${{ github.sha }}'
            build_id = 'tauri-ui-${{ github.run_id }}-${{ github.run_attempt }}'
            run_id = '${{ github.run_id }}'
            run_attempt = '${{ github.run_attempt }}'
            ui_sha256 = (Get-FileHash $ui -Algorithm SHA256).Hash.ToLowerInvariant()
            ui_size = $ui.Length
            cargo_lock_sha256 = (Get-FileHash $lock -Algorithm SHA256).Hash.ToLowerInvariant()
            cargo_lock_size = $lock.Length
            zip_sha256 = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLowerInvariant()
            zip_size = $zip.Length
          } | ConvertTo-Json | Set-Content -Encoding utf8 dist/tauri-ui-build-receipt.json

      - name: Attest Tauri UI ZIP provenance
        uses: actions/attest@v4
        with:
          subject-path: dist/fairypam-agent-tauri-ui-windows.zip

      - name: Attest Tauri UI receipt provenance
        uses: actions/attest@v4
        with:
          subject-path: dist/tauri-ui-build-receipt.json

      - name: Upload non-promotable Tauri UI artifact
        uses: actions/upload-artifact@v7
        with:
          name: fairypam-agent-tauri-ui-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            dist/fairypam-agent-tauri-ui-windows.zip
            dist/tauri-ui-build-receipt.json
          if-no-files-found: error
          retention-days: 14

  verify-dev-attestation:
    name: Verify Dev artifact attestation as a consumer
    needs: dev-automation
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - name: Download Dev artifact
        uses: actions/download-artifact@v5
        with:
          name: fairypam-agent-dev-${{ github.run_id }}-${{ github.run_attempt }}
          path: dist/verified-dev

      - name: Verify Dev ZIP and receipt provenance
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          $signer = "$env:GITHUB_REPOSITORY/.github/workflows/windows-candidate.yml"
          gh attestation verify dist/verified-dev/fairypam-agent-dev-windows.zip --repo $env:GITHUB_REPOSITORY --signer-workflow $signer --deny-self-hosted-runners
          gh attestation verify dist/verified-dev/dev-build-receipt.json --repo $env:GITHUB_REPOSITORY --signer-workflow $signer --deny-self-hosted-runners

  verify-tauri-ui-attestation:
    name: Verify Tauri UI artifact attestation as a consumer
    needs: tauri-ui
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - name: Download Tauri UI artifact
        uses: actions/download-artifact@v5
        with:
          name: fairypam-agent-tauri-ui-${{ github.run_id }}-${{ github.run_attempt }}
          path: dist/verified-tauri-ui

      - name: Verify Tauri UI ZIP and receipt provenance
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $PSNativeCommandUseErrorActionPreference = $true
          $signer = "$env:GITHUB_REPOSITORY/.github/workflows/windows-candidate.yml"
          gh attestation verify dist/verified-tauri-ui/fairypam-agent-tauri-ui-windows.zip --repo $env:GITHUB_REPOSITORY --signer-workflow $signer --deny-self-hosted-runners
          gh attestation verify dist/verified-tauri-ui/tauri-ui-build-receipt.json --repo $env:GITHUB_REPOSITORY --signer-workflow $signer --deny-self-hosted-runners
