; Single authority for the Windows product layout. Rust build scripts read this
; definition too, so the installer and its verifier always agree on the root.
!define FAIRYPAM_INSTALL_DIRECTORY "FairyPam"
!define FAIRYPAM_INSTALL_BOOTSTRAP_DIRECTORY ".fairypam-installer"
